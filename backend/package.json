{
  "name": "arthistory-api",
  "version": "0.1.0",
  "private": true,
  "description": "Art history API (/v1) and admin panel (/admin)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "PORT=3005 node --watch src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "helmet": "^8.3.0"
  }
}
